{% extends 'base.html.twig' %}

{% block contenu %}
<div id="conteneur_principal" class="horizontal">
    <img src="assets/img/Grille.webp" alt="Exemple de grille de Sudoku">
    <section class="formulaire">
        <h1>Créer un compte</h1>
        <form method="post">
            <div>
                <label for="pseudo">Pseudo</label>
                <input id="pseudo"
                    {% if erreurs.pseudo is defined %}class="formulaire_erreur"{% endif %}
                    name="pseudo"
                    type="text"
                    placeholder="Pseudo..."
                    autocomplete="username"
                    {% if post.pseudo is defined %}value="{{ post.pseudo }}"{% endif %}
                    required
                />
                {% if erreurs.pseudo is defined %}<p class="formulaire_erreur">{{ erreurs.pseudo }}</p>{% endif %}
            </div>
            <div>
                <label for="email">Adresse mail</label>
                <input id="email"
                    {% if erreurs.email is defined %}class="formulaire_erreur"{% endif %}
                    name="email"
                    type="email"
                    placeholder="exemple@exemple.fr"
                    autocomplete="email"
                    {% if post.email is defined %}value="{{ post.email }}"{% endif %}
                    required
                />
                {% if erreurs.email is defined %}<p class="formulaire_erreur">{{ erreurs.email }}</p>{% endif %}
            </div>
            <div>
                <label for="mdp">Mot de passe</label>
                <input id="mdp"
                    {% if erreurs.mdp is defined %}class="formulaire_erreur"{% endif %}
                    name="mdp"
                    type="password"
                    placeholder="*********"
                    autocomplete="off"
                    required
                />
                {% if erreurs.mdp is defined %}<p class="formulaire_erreur">{{ erreurs.mdp }}</p>{% endif %}
            </div>
            <div>
                <label for="mdp_confirm">Confirmation du mot de passe</label>
                <input id="mdp_confirm"
                    {% if erreurs.mdp_confirm is defined %}class="formulaire_erreur"{% endif %}
                    name="mdp_confirm"
                    type="password"
                    placeholder="*********"
                    autocomplete="off"
                    required
                />
                {% if erreurs.mdp_confirm is defined %}<p class="formulaire_erreur">{{ erreurs.mdp_confirm }}</p>{% endif %}
            </div>
            <div class="conditions">
                <div>
                    <input id="conditions"
                        {% if erreurs.conditions is defined %}class="formulaire_erreur"{% endif %}
                        type="checkbox"
                        name="conditions"
                        required
                    />
                    <label for="conditions">J'accepte les <a href="mentions" target="_blank">termes d'utilisation</a></label>
                </div>
                {% if erreurs.conditions is defined %}<p class="formulaire_erreur">{{ erreurs.conditions }}</p>{% endif %}
            </div>
            <input type="hidden" name="tokenCSRF" value="{{ tokenCSRF }}" />
            {% if erreurs.general is defined %}<p class="formulaire_erreur">{{ erreurs.general }}</p>{% endif %}
            <input type="submit" class="bouton boutonPrincipal boutonDefaut" value="Créer mon compte" />
        </form>
    </section>
</div>
{% endblock %}